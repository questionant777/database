spring:
  datasource:
    url: jdbc:h2:mem:testdb
    initialization-mode: always
  profiles:
    active: test

  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none

jwt:
  secret: secret123

logging:
  level:
    ROOT: ERROR
