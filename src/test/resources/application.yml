spring:
  datasource:
    url: jdbc:h2:mem:testdb
    initialization-mode: always
  profiles:
    active: test
  shell:
    interactive:
      enabled: false

  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none

logging:
  level:
    ROOT: ERROR